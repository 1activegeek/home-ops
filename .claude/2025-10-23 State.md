Perfect! Now let me create a summary document for next session:
Session Summary: Rook-Ceph Raw Volumes Deployment
✅ Major Accomplishments
Talos Raw Volumes Configuration
Fixed raw-volumes.yaml to use correct RawVolumeConfig format
Successfully provisioned raw volumes on all 3 nodes (969 GB each)
Device path: /dev/disk/by-partlabel/r-ceph → /dev/nvme0n1p5
Ceph Metadata Cleanup
Completely wiped old Ceph signatures from partitions
Used talosctl wipe disk nvme0n1p5 --drop-partition to fully remove partitions
Recreated clean, unformatted partitions without old metadata
Rook-Ceph Configuration
helmrelease.yaml correctly references /dev/disk/by-partlabel/r-ceph
Added enableCSIOperator: false to operator HelmRelease (but not yet applied to deployment)
❌ Remaining Issues
Rook Operator CSI Mode (Primary Blocker)
Operator defaults to CSI operator mode despite HelmRelease setting
Blocks cluster initialization (stuck at "Detecting Ceph version")
Fix: kubectl -n rook-ceph set env deployment/rook-ceph-operator ROOK_CSI_ENABLE_OPERATOR=false
Pod Issues (Need Investigation)
rook-discover-rk9x5: CrashLoopBackOff
ceph-csi-controller-manager: Restarting
Transient health probe failures on spegel pods
Some Flux/external-secrets warnings
📋 Next Session: Action Plan
Step 1: Fix Rook Operator
kubectl -n rook-ceph set env deployment/rook-ceph-operator ROOK_CSI_ENABLE_OPERATOR=false
kubectl -n rook-ceph delete pod -l app=rook-ceph-operator
Step 2: Check CephCluster Deployment
kubectl -n rook-ceph get cephcluster
kubectl -n rook-ceph get pods | grep -E "(mon|mgr|osd)"
Step 3: Investigate Pod Issues
kubectl -n rook-ceph logs rook-discover-rk9x5
kubectl -n rook-ceph describe pod rook-discover-rk9x5
Step 4: Monitor OSD Deployment
kubectl -n rook-ceph logs job/rook-ceph-osd-prepare-asgard-mpc-01
kubectl -n rook-ceph get pods -l app=rook-ceph-osd
🎯 Expected Outcome
With clean partitions and correct CSI configuration, OSDs should deploy successfully and Ceph should reach HEALTH_OK status with 3 OSDs (969GB each).
📁 Key Files Modified
talos/talconfig.yaml - Raw volumes patch enabled
talos/patches/global/raw-volumes.yaml - RawVolumeConfig
kubernetes/apps/rook-ceph/rook-ceph/app/helmrelease.yaml - enableCSIOperator: false
kubernetes/apps/rook-ceph/rook-ceph/cluster/helmrelease.yaml - Device paths
All background processes cleaned up. Ready to compact!