---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: shlink
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-store
  target:
    name: shlink-secret
  dataFrom:
    - extract:
        key: shlink
