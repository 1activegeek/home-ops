---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: agentbox
spec:
  chartRef:
    kind: OCIRepository
    name: agentbox
  interval: 1h
  values:
    controllers:
      main:
        # Recreate (not RollingUpdate): the single RWO config PVC can't be
        # held by two pods at once, so the old pod must release before the new
        # one attaches.
        strategy: Recreate
        containers:
          app:
            image:
              # NOTE: verify the current Playwright image tag before merge.
              repository: mcr.microsoft.com/playwright
              tag: v1.49.1-jammy
            command: ["/bin/bash", "/scripts/entrypoint.sh"]
            env:
              TZ: America/Denver
              TS_HOSTNAME: agentbox
              HOME: /home/pwuser
              NPM_CONFIG_PREFIX: /home/pwuser/.npm-global
              PLAYWRIGHT_BROWSERS_PATH: /home/pwuser/.cache/ms-playwright
              TS_AUTHKEY:
                valueFrom:
                  secretKeyRef:
                    name: agentbox-secret
                    key: ts_authkey
            resources:
              requests:
                cpu: "1"
                memory: 2Gi
              limits:
                cpu: "4"
                memory: 8Gi
                ephemeral-storage: 10Gi
        pod:
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            fsGroup: 1000

    persistence:
      # Disposable workstation home (CLI auth, config, caches, session state).
      # storageClass longhorn; MUST be excluded from the Longhorn backup group
      # (auth tokens must never reach the NAS backup) — see Plan 1 task 8.
      config:
        type: persistentVolumeClaim
        storageClass: longhorn
        accessMode: ReadWriteOnce
        size: 20Gi
        globalMounts:
          - path: /home/pwuser
      # Chromium shared memory.
      shm:
        type: emptyDir
        medium: Memory
        sizeLimit: 1Gi
        globalMounts:
          - path: /dev/shm
      # Boot entrypoint.
      scripts:
        type: configMap
        name: agentbox-scripts
        defaultMode: 0755
        globalMounts:
          - path: /scripts
      # Obsidian vault (SyncThing replica). Export root verified: /volume1/syncthing.
      vault:
        type: nfs
        server: ${NFS_SERVER}
        path: /volume1/syncthing/ic_obsidian/lifeOS
        globalMounts:
          - path: /data/lifeOS
      # Full iCloud Files PARA tree (SyncThing replica).
      documents:
        type: nfs
        server: ${NFS_SERVER}
        path: /volume1/syncthing/ic_documents
        globalMounts:
          - path: /data/documents

    # No service, no route: internal-only by absence. Reachable solely via
    # Tailscale SSH (in-container, userspace) and the outbound Claude relay.
