---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: homarr
  namespace: default
spec:
  chartRef:
    kind: OCIRepository
    name: homarr
  interval: 1h
  timeout: 15m
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    image:
      repository: ghcr.io/homarr-labs/homarr
      tag: "8.2.1"
    env:
      AUTH_PROVIDERS: "credentials"
      TZ: "America/New_York"
    # envSecrets:
    #   SECRET_ENCRYPTION_KEY:
    #     secretName: {{ .Release.Name}}-secret
    #     secretKey: SECRET_ENCRYPTION_KEY
    #   HOMARR_PASSWORD:
    #     secretName: {{ .Release.Name}}-secret
    #     secretKey: HOMARR_PASSWORD
    httproute:
      enabled: true
      hostnames:
        - homarr.${SECRET_DOMAIN}
        - home.${SECRET_DOMAIN}
      parentRefs:
        - name: envoy-internal
          namespace: network
          sectionName: https
    persistence:
      homarrDatabase:
        enabled: true
        size: 5Gi
        # storageClassName: "Longhorn" # Use default (Longhorn)
