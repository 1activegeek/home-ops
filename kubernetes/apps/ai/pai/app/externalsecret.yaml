---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pai
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-store
  target:
    name: pai-secret
  dataFrom:
    - extract:
        key: pai
