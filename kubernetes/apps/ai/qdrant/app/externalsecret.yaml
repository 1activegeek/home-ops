---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: qdrant
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-store
  target:
    name: qdrant-secret
  dataFrom:
    - extract:
        key: qdrant
