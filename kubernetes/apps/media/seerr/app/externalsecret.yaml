---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: seerr
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-store
  target:
    name: seerr-secret
    template:
      data:
        PLEX_TOKEN: "{{ .plex_token }}"
  dataFrom:
    - extract:
        key: seerr
