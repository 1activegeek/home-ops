{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "packageRules": [
    {
      "description": "Pin onepassword-connect chart to 2.2.1 - chart 2.3.0 changed credentials mounting from base64 env var to raw JSON file, incompatible with current SOPS secret encoding. Fix by re-encoding the 1password-credentials.json SOPS secret as raw JSON before upgrading.",
      "matchPackageNames": ["connect"],
      "matchManagers": ["helmv3", "helm-values"],
      "matchCurrentVersion": "2.2.1",
      "enabled": false
    }
  ]
}
